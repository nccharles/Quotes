import { TexthighlightDirective } from './texthighlight.directive';

describe('TexthighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TexthighlightDirective();
    expect(directive).toBeTruthy();
  });
});
